import { Router } from 'express';
import ### from '../app/Http/Controllers/###.js';

export default (function () {

    const router = Router();

    router.post("/task", ###);

    return router;

})();